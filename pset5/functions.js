/*
function greetName() {
    let greeting = "Bonjour, "
    let nameToPrint = "Emma"
    print(greeting + nameToPrint)
}

greetName()

function crime(crime, isConvicted) {
    print ("Emma did " +crime+ " today!")
    if (isConvicted) {print("Enjoy jail!")}
    else {print("And didn't get caught!")}
}
*/

//crime("manslaughter") 

function square(numToSquare) {
    let squaredNum = numToSquare * numToSquare
    print(squaredNum)
}

square(4)

print(squaredNum)

